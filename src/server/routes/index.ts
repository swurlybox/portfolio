/*
Importing the "routes" folder makes all exported objects
in all files under the "routes" folder available to the caller.

This index.ts file serves as a way of renaming the exported objects.
*/
export {default as root} from "./root";
export {default as sandbox} from "./sandbox";